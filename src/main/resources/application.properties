spring.application.name=zzf-questions

# 数据库配置
spring.datasource.url=jdbc:mysql://localhost:3306/questions?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=Zzf5201314.

# JPA配置
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# 数据库初始化配置
# 可选值: always(总是执行), embedded(仅嵌入式数据库), never(从不执行)
spring.sql.init.mode=never
# spring.sql.init.schema-locations=classpath:sql/init.sql
# 数据初始化脚本（如果需要初始数据）
# spring.sql.init.data-locations=classpath:sql/data.sql

# 服务器配置
server.port=9090
server.servlet.context-path=/
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true

# 日志配置
logging.level.com.exam=DEBUG
logging.level.org.springframework.web=DEBUG